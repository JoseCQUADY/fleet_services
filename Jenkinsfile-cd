pipeline {
    agent any
    stages {
        stage('Test SSH Connection') {
            steps {
                sshagent(['oracle-ssh-key']) {
                    sh '''
                        ssh -o StrictHostKeyChecking=no user@servidor_remoto "echo Conexion SSH exitosa"
                    '''
                }
            }
        }
    }
}
